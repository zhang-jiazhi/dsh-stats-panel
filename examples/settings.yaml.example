# 渠道配置示例（追加到 ~/.dsh/settings.yaml）

```yaml
llm-pi-ai:
  providers:
    opencode-go:
      apiKeyEnv: OPENCODE_GO_API_KEY
    mimo:
      apiKeyEnv: XIAOMI_API_KEY
      displayName: Xiaomi Mimo
      api: openai-completions
      baseURL: https://token-plan-cn.xiaomimimo.com/v1
      compat:
        thinkingFormat: deepseek
        supportsReasoningEffort: true
      models:
        - id: mimo-v2.5-pro
          name: Mimo v2.5 Pro
    kimi:                                     # 示例：Kimi 官方按量渠道
      apiKeyEnv: MOONSHOT_API_KEY
      api: openai-completions
      baseURL: https://api.moonshot.cn/v1
      models:
        - id: kimi-k2.5
          name: Kimi K2.5
```

分桶日历（可选，默认跟随主机本地时区）：

```yaml
stats-panel:
  dayBoundary: local   # local(默认) | utc | +08:00 | 480（分钟）
```

凭据放入 `~/.dsh/.credentials.yaml`（或环境变量）：

```yaml
DEEPSEEK_API_KEY: sk-...
OPENCODE_GO_API_KEY: sk-...
XIAOMI_API_KEY: tp-...
MOONSHOT_API_KEY: sk-...
OPENROUTER_API_KEY: sk-or-...
SILICONFLOW_API_KEY: sk-...
STEPFUN_API_KEY: sk-...
NOVITA_API_KEY: sk-...
OPENAI_API_KEY: sk-...
ANTHROPIC_API_KEY: sk-ant-...
```

配置后重启 `dsh web`，在设置 → Token 使用统计 → 渠道余量/余额 中即可看到对应渠道的自动查询结果。
